name = "sone-plantnet-proxy"
main = "worker.js"
compatibility_date = "2024-11-01"

# ملاحظة: لا تحط مفتاح PlantNet هنا.
# المفتاح يُضاف كسرّ (secret) من طرفين:
#   1. يدوياً عبر: wrangler secret put PLANTNET_API_KEY
#   2. أو تلقائياً عبر GitHub Actions (موضّح بملف الـ workflow)
